apiVersion: apps/v1
kind: Deployment
metadata:
  name: n3nai
  namespace: homepage-n3n-ai
spec:
  replicas: 2
  selector:
    matchLabels:
      app: n3nai
  template:
    metadata:
      labels:
        app: n3nai
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: n3nai
          image: ghcr.io/n3n-dev/n3nai:latest
          ports:
            - containerPort: 3000
---
apiVersion: v1
kind: Service
metadata:
  name: n3nai-svc
  namespace: homepage-n3n-ai
spec:
  selector:
    app: n3nai
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: n3nai-ingress
  namespace: homepage-n3n-ai
spec:
  ingressClassName: traefik
  rules:
    # 스테이징/검증용 내부 호스트 (와일드카드 *.prod.nthreen.com → prod 클러스터)
    # n3n.ai DNS cutover 전까지 이 호스트로 검증한다.
    - host: homepage-n3n-ai.prod.nthreen.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n3nai-svc
                port:
                  number: 80
    - host: n3n.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n3nai-svc
                port:
                  number: 80
    - host: www.n3n.ai
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: n3nai-svc
                port:
                  number: 80
